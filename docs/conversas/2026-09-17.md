# Continuação de 17/09/2026

Resumo fiel, não transcrição integral.

- Continuação do backup externo autorizado no dia anterior. Usuário aceitou Google Drive e informou que o computador de backup fica desligado em alguns períodos. Foi perguntado se prefere execução pendente ao religar ou execução diária na nuvem; resposta ainda pendente.
- Implementação local concluída: AES-256-GCM com autenticação, upload do dump e manifesto via rclone, leitura de volta e conferência SHA-256, registro de sucesso condicionado ao upload, verificação opcional da cópia externa, comando de descriptografia e webhook HTTPS opcional para falhas. Modelos systemd e procedimento de recuperação atualizados.
- `npm run test:backup-job` e checagem Prettier aprovaram. Testes usaram somente arquivos fictícios; nenhuma conexão com Google Drive, backup real, agendamento, webhook ou migração foi executado. A proteção automática dos dados reais ainda não está ativa.

- Usuário escolheu executar o backup pendente quando o computador ligar e confirmou Google Drive; computador desliga frequentemente. Ambiente é Windows com WSL Ubuntu, systemd de usuário funcional; `Linger=no`. Preparados `scripts/backup-local.mjs`, configuração privada `.local/backup.env` com chave aleatória 256 bits (não exibida), rclone local 1.60.1 do pacote Ubuntu e script PowerShell de tarefa diária/ao logon. `rclone.conf` OAuth ainda ausente; tarefa Windows e timers Linux não instalados/ativados. Verificação sintática do PowerShell e `systemd-analyze verify` passaram. Usuário precisa guardar a chave fora do computador e criar Client ID próprio do Google Drive; o compartilhado do rclone será aposentado em 2026. Nenhum dump real executado.
- Usuário confirmou venda somente por unidade/embalagem inteira e estoque disponível ao cancelar antes do pagamento. Implementação existente baixa estoque apenas no primeiro recebimento, então cancelar antes mantém saldo. Teste de regressão adicionado em `tests/product-sales.mjs`; primeira suíte de integração falhou apenas porque o sandbox bloqueou socket TCP do PostgreSQL isolado, repetição escalada em andamento neste marco. Nenhuma alteração funcional da API, migration ou dado real.

- A resposta posterior do usuário escolheu execução diária **na nuvem**, substituindo a opção local. Modelos Windows/systemd permaneceram apenas preparados; nenhuma tarefa foi instalada. Workflow GitHub Actions adicionado com execução às 03h17 de São Paulo, gate `BACKUP_ENABLED=true` ainda desligado, instalação de PostgreSQL 17/rclone, dump criptografado e verificado no Drive. Nenhum segredo foi enviado ao GitHub e não houve push nem execução real. Guia em `docs/backup-cloud-drive.md`. Google Drive OAuth ainda não conectado; usuário pediu instruções passo a passo. Orientado a criar projeto Google Cloud, ativar Drive API, criar e publicar app OAuth Desktop, configurar remoto `drive_salao` e guardar a chave fora do PC sem compartilhá-la.
- Usuário perguntou sobre migrar depois banco e Drive para outro computador. Explicado que cópias cifradas e chave permitem recuperar em outra máquina; migrar o banco exige janela de corte e reconfigurar conexão/backup. Usuário pediu esquecer comanda e status da agenda; o teste temporário de comanda e mudança de texto foram removidos. A suíte de integração escalada passou em PostgreSQL isolado, incluindo build. Teste de backup com fixtures, Prettier, `git diff --check`, YAML por PyYAML, sintaxe PowerShell e verificação dos modelos systemd passaram. Primeira tentativa de integração falhou somente porque o sandbox bloqueou socket TCP; repetição aprovada. Nenhum dado real acessado ou alterado por testes. Sem migration, sem processos ativos.
- Usuário pediu salvar a conversa no ponto de conectar Google Drive para continuar mais tarde, após encerrar os testes. Próximo passo: acompanhamento de OAuth e preservação da chave; depois configurar Secrets/variável do GitHub, publicar workflow, testar manualmente backup real e restauração isolada, alertas e política de retenção. Não afirmar proteção automática ativa.

- Usuário pediu encerrar tudo que estivesse rodando. Conferidos processos e portas dentro e fora do sandbox: não havia servidor web/API, teste, PostgreSQL temporário ou rotina de backup do projeto em execução. Somente DNS do ambiente aparecia nas portas abertas; nada do projeto precisou ser encerrado. Nenhum timer de backup foi ativado.

- Retomada da conexão Google Drive: usuário criou projeto no Google Cloud e informou que Google Drive API está ativada. OAuth configurado como Externo/Testando; orientado a incluir a própria conta Drive como usuária de teste. Como não possui domínio, criou o repositório público `SantosMarcos95/santosmarcos95.github.io` (confirmado com `gh repo view`). Rascunhos locais das páginas de apresentação, privacidade e termos em `.local/oauth-pages/`, ainda não publicados. Google Drive ainda não autorizado no rclone; nenhuma credencial foi recebida, nenhum Secret configurado e o backup continua inativo.

- Usuário autorizou publicação das três páginas. Commit `adf81e7` enviado ao repositório público `SantosMarcos95/santosmarcos95.github.io`; GitHub Pages reportou `status: built`, origem `main` e HTTPS ativo. Verificação pública retornou HTTP 200 para `/`, `/privacidade.html` e `/termos.html`, com o texto esperado. Próximo passo é inserir as três URLs e o domínio no Branding do Google Auth Platform; verificação do domínio e publicação OAuth ainda não confirmadas. Nenhum dado real de backup ou credencial foi enviado.

- Usuário informou que adicionou o domínio autorizado `santosmarcos95.github.io` e gravou os três links no Branding. Isso foi relato do usuário; ainda não há acesso do assistente ao Google Cloud para verificação independente. Próximo passo orientado: verificar em Público-alvo se o botão Publicar app está disponível; OAuth continua relatado como Externo/Testando até confirmação posterior.

- Usuário relatou app OAuth publicado e cliente Desktop criado, sem compartilhar credenciais. Tentativas de autorizar o remoto `drive_salao` no WSL falharam primeiro com `State did not match` (instrução anterior do assistente havia omitido o parâmetro `?state`, corrigida) e depois duas vezes com `No code returned by remote server`, em cerca de dois segundos. Inspeção local somente booleana confirmou remoto Drive com escopo `drive.file`, arquivo 0600 e token ausente. Rclone local Ubuntu `1.60.1-DEV` foi substituído pela versão oficial `1.75.1` em `.local/bin/rclone`, com ZIP baixado do site oficial e SHA-256 conferido com `SHA256SUMS`; binário anterior preservado em `.local/bin/rclone-1.60.1`. Ainda não houve tentativa de autorização com a versão nova nem backup real.

- Nova tentativa com rclone Linux 1.75.1 falhou em dois segundos com o mesmo `No code returned by remote server`; o `xdg-open` nem estava disponível. Preparada alternativa Windows: binário oficial 1.75.1 em `.local/bin/rclone.exe`, SHA-256 conferido; execução por interoperabilidade WSL confirmada e `listremotes` do arquivo privado via caminho UNC mostrou `drive_salao:`. Autorização Windows ainda não executada. Nenhum token registrado.

- Autorização pelo executável Windows chegou ao Google, que respondeu `401 invalid_client` / cliente OAuth não encontrado. Verificação local limitada a presença/formato (sem exibir valores) mostrou `client_id` presente, porém sem o sufixo esperado `.apps.googleusercontent.com`; `client_secret` presente, token ausente. Próximo passo é usuário conferir e corrigir localmente Client ID e Client Secret do cliente Desktop no Google Cloud, sem enviá-los pela conversa, e repetir autorização Windows. Conta Google não registrada neste resumo.

- Usuário corrigiu o Client ID localmente; verificação apenas de formato confirmou sufixo correto, Secret presente e escopo `drive.file`. Autorização Windows exibiu `Success!`; inspeção booleana confirmou token salvo. A gravação via Windows mudou modo de `.local/rclone.conf` para 0644; corrigido imediatamente para 0600. Teste com arquivo aleatório fictício em `drive_salao:backups` passou: upload, download e `cmp` byte a byte (exit 0); arquivo fictício removido do Drive. Primeira tentativa de upload no sandbox ficou sem resposta de rede e foi interrompida; repetição fora do sandbox passou. Chave local de 64 caracteres existe em `.local/backup.env` com modo 0600, ainda sem confirmação de cópia fora do computador. Nenhum backup do banco real, GitHub Secret ou agendamento ativo foi executado.

- Usuário confirmou que guardou a chave fora do computador; a localização/valor não foram registrados. Três GitHub Actions Secrets (`BACKUP_DATABASE_URL`, `BACKUP_ENCRYPTION_KEY`, `BACKUP_RCLONE_CONFIG`) cadastrados por `gh` a partir de arquivos locais sem imprimir valores; `gh secret list` confirmou os nomes. Repositório principal é público, branch padrão `main`, variável `BACKUP_ENABLED` ausente. Workflow ajustado para permitir teste manual com agendamento desligado e instalar rclone oficial 1.75.1 com SHA-256 fixado. YAML analisado por PyYAML, `git diff --check` passou; arquivos privados continuam ignorados pelo Git. Próximo passo: publicar workflow, executar manualmente, validar cópia real e restauração isolada antes de ativar rotina diária. Sem backup real até este marco.

- Workflow publicado em `main` nos commits `a78605c` e `102ff74`; GitHub o reconheceu. Primeira execução manual `35259782418` instalou ferramentas e leu Secrets, mas falhou imediatamente no comando de backup. Diagnóstico local sem valores mostrou URL Neon com parâmetros `sslmode` e `channel_binding`; `scripts/lib/backup.mjs` rejeitava `channel_binding`, embora seja parâmetro libpq válido. Permitido esse parâmetro e adicionado ao ensaio isolado com valor `disable`. Primeira tentativa de `test:backup` falhou só por porta TCP bloqueada no sandbox; repetição fora do sandbox passou integralmente, incluindo build, integração, backup/restauração e origem intacta. Correção ainda não publicada neste registro. Nenhum backup real foi confirmado; agenda diária desligada.

- Correção `8d024de` publicada; segunda execução manual `35260454195` também falhou antes de gerar cópia. Diagnóstico sem dados mostrou incompatibilidade: Neon PostgreSQL 18.6, pg_dump do runner 17.11. Documentação oficial confirma que pg_dump não atende servidor de versão major mais nova. Cliente PostgreSQL 18.6 extraído localmente do pacote oficial PGDG Ubuntu 24.04; `pg_dump --schema-only` contra Neon passou sem avisos, sem exportar dados. Workflow alterado localmente para instalar/usar cliente 18, ainda não publicado neste marco. Nenhum backup real confirmado; agendamento desligado.

- Ajuste para cliente PostgreSQL 18 publicado em `dc41637`. Terceira execução manual `35262437640` concluiu com sucesso no GitHub, incluindo dump real, criptografia, cópia para Google Drive, leitura de volta e verificação. O par cifrado e manifesto `backup-bIqiYU` foi baixado para ensaio; hash do manifesto conferiu com dump descriptografado, `pg_restore --list` passou e restauração em banco PostgreSQL 18.6 isolado passou. Consulta agregada no banco restaurado: 37 tabelas, 13 migrations, 412 constraints e 19 triggers. Nenhuma escrita no Neon. Banco temporário parado, porta 15441 fechada e diretório de recuperação com dump claro apagado.
- Usuário autorizou concluir essa parte sem novas perguntas. Variável GitHub `BACKUP_ENABLED=true` criada e conferida em 17/09 às 19:38 UTC. Workflow diário às 03h17 de São Paulo ativo; primeira execução automática ainda não ocorreu. Chave foi guardada fora do PC segundo confirmação do usuário; três Secrets GitHub já cadastrados. Monitor de ausência de execução, retenção no Drive e alerta externo continuam pendentes; verificar próxima execução automática e notificações de falha da conta GitHub.

- Nova prioridade solicitada para o cronograma: colocar em operação lembrete por WhatsApp ao cliente 24 horas antes de cada serviço agendado e resumo da agenda do dia seguinte para cada funcionário diariamente às 10h (horário de São Paulo). Registrada em `docs/roadmap.md` como próxima etapa, com exemplo de 18/09/2026 às 16h → 17/09/2026 às 16h. Ainda não houve implementação, configuração de provedor ou envio.

- Usuário decidiu usar número novo exclusivamente para mensagens automáticas do sistema; funcionários continuam usando o número WhatsApp Business atual para atendimento. Planejada integração direta com Cloud API da Meta, sem coexistência nem provedor escolhido. Será necessário tratar respostas recebidas no número automático e orientar o cliente ao contato humano. Nenhuma contratação, número novo ou integração ativada.

- Usuário pediu adiar a implementação de WhatsApp porque ainda não possui o número novo. Retomar após obter o número; prosseguir com verificação da primeira execução agendada do backup, retenção/alertas e validação operacional dos módulos publicados. Nenhuma mudança de código ou configuração de envio nesta decisão.

- Usuário autorizou avançar na proteção do backup. Preparados monitor GitHub Actions separado às 11h17 de São Paulo (alerta por falha se não houver par completo no Drive em até 36h) e retenção de pares completos com mais de 90 dias, preservando ao menos os 7 mais recentes e só após novo backup confirmado. Testes isolados passaram; listagem real somente leitura mostrou o par cifrado e a checagem de idade passou. Nenhum arquivo real foi apagado. Publicação e ensaio do workflow monitor ainda pendentes neste marco.
