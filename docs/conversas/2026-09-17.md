# Continuação de 17/09/2026

Resumo fiel, não transcrição integral.

- Continuação do backup externo autorizado no dia anterior. Usuário aceitou Google Drive e informou que o computador de backup fica desligado em alguns períodos. Foi perguntado se prefere execução pendente ao religar ou execução diária na nuvem; resposta ainda pendente.
- Implementação local concluída: AES-256-GCM com autenticação, upload do dump e manifesto via rclone, leitura de volta e conferência SHA-256, registro de sucesso condicionado ao upload, verificação opcional da cópia externa, comando de descriptografia e webhook HTTPS opcional para falhas. Modelos systemd e procedimento de recuperação atualizados.
- `npm run test:backup-job` e checagem Prettier aprovaram. Testes usaram somente arquivos fictícios; nenhuma conexão com Google Drive, backup real, agendamento, webhook ou migração foi executado. A proteção automática dos dados reais ainda não está ativa.

- Usuário escolheu executar o backup pendente quando o computador ligar e confirmou Google Drive; computador desliga frequentemente. Ambiente é Windows com WSL Ubuntu, systemd de usuário funcional; `Linger=no`. Preparados `scripts/backup-local.mjs`, configuração privada `.local/backup.env` com chave aleatória 256 bits (não exibida), rclone local 1.60.1 do pacote Ubuntu e script PowerShell de tarefa diária/ao logon. `rclone.conf` OAuth ainda ausente; tarefa Windows e timers Linux não instalados/ativados. Verificação sintática do PowerShell e `systemd-analyze verify` passaram. Usuário precisa guardar a chave fora do computador e criar Client ID próprio do Google Drive; o compartilhado do rclone será aposentado em 2026. Nenhum dump real executado.
- Usuário confirmou venda somente por unidade/embalagem inteira e estoque disponível ao cancelar antes do pagamento. Implementação existente baixa estoque apenas no primeiro recebimento, então cancelar antes mantém saldo. Teste de regressão adicionado em `tests/product-sales.mjs`; primeira suíte de integração falhou apenas porque o sandbox bloqueou socket TCP do PostgreSQL isolado, repetição escalada em andamento neste marco. Nenhuma alteração funcional da API, migration ou dado real.

- A resposta posterior do usuário escolheu execução diária **na nuvem**, substituindo a opção local. Modelos Windows/systemd permaneceram apenas preparados; nenhuma tarefa foi instalada. Workflow GitHub Actions adicionado com execução às 03h17 de São Paulo, gate `BACKUP_ENABLED=true` ainda desligado, instalação de PostgreSQL 17/rclone, dump criptografado e verificado no Drive. Nenhum segredo foi enviado ao GitHub e não houve push nem execução real. Guia em `docs/backup-cloud-drive.md`. Google Drive OAuth ainda não conectado; usuário pediu instruções passo a passo. Orientado a criar projeto Google Cloud, ativar Drive API, criar e publicar app OAuth Desktop, configurar remoto `drive_salao` e guardar a chave fora do PC sem compartilhá-la.
- Usuário perguntou sobre migrar depois banco e Drive para outro computador. Explicado que cópias cifradas e chave permitem recuperar em outra máquina; migrar o banco exige janela de corte e reconfigurar conexão/backup. Usuário pediu esquecer comanda e status da agenda; o teste temporário de comanda e mudança de texto foram removidos. A suíte de integração escalada passou em PostgreSQL isolado, incluindo build. Teste de backup com fixtures, Prettier, `git diff --check`, YAML por PyYAML, sintaxe PowerShell e verificação dos modelos systemd passaram. Primeira tentativa de integração falhou somente porque o sandbox bloqueou socket TCP; repetição aprovada. Nenhum dado real acessado ou alterado por testes. Sem migration, sem processos ativos.
- Usuário pediu salvar a conversa no ponto de conectar Google Drive para continuar mais tarde, após encerrar os testes. Próximo passo: acompanhamento de OAuth e preservação da chave; depois configurar Secrets/variável do GitHub, publicar workflow, testar manualmente backup real e restauração isolada, alertas e política de retenção. Não afirmar proteção automática ativa.
