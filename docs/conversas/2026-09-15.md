# Continuação de 15/09/2026

Resumo fiel da sessão, não transcrição integral.

### Preparação do deploy Vercel

- Usuário informou que criou uma variável no painel da Vercel; o valor não foi acessado ou salvo nos registros.
- Criado `apps/api/src/bootstrap.ts` para compartilhar a configuração NestJS entre o servidor local e a função serverless `api/index.ts`. Criado `vercel.json` para build do monorepo, frontend em `apps/web/dist`, função API e fallback do React Router.
- Adicionado `vercel-build` com `prisma generate` antes do build. `npm run typecheck`, `npm run build` e `npm run vercel-build` passaram.
- Nenhum banco local/real foi iniciado ou alterado; nenhuma migration, backup, dado ou credencial foi enviado nesta etapa.
- A configuração depende de `DATABASE_URL` e `WEB_ORIGIN`; a segunda deve apontar para a origem HTTPS pública da aplicação. Deploy externo e smoke test ainda pendentes.
- Commit `802c79a` criado e enviado para `origin/main`, disparando o fluxo de deploy da Vercel. A consulta posterior ao GitHub por DNS no sandbox falhou; não foi interpretada como falha do push.
- O primeiro build Vercel com a raiz correta falhou porque o bundler compilou diretamente os fontes NestJS importados pela função e rejeitou `@Get()` como decorator padrão. `api/index.ts` foi ajustado para importar `apps/api/dist/bootstrap.js`, produzido antes pela compilação da API. `npm run vercel-build` local passou após o ajuste; novo commit/deploy pendente.
- Após o deploy `b2168bf`, a página respondeu 200, mas `/api/health` respondeu 404. A causa observada foi roteamento: a função não recebia os subcaminhos `/api/*`. `vercel.json` recebeu rotas explícitas para `/api` e `/api/*`, com fallback estático preservado; novo deploy pendente.

- Usuário pediu para continuar o projeto. Lidos retomada, roadmap e procedimento de backup.
- Encontrados `scripts/backup-job.mjs`, `scripts/lib/backup-job.mjs`, `tests/backup-job.mjs` e comandos npm já presentes, ainda sem documentação de continuidade. Preservados e revisados.
- Reforçada validação do caminho: o último backup deve ser pasta filha direta do diretório configurado, com prefixo `backup-`. Adicionado cenário para caminhos externos e subpastas.
- `npm run test:backup-job` passou antes e depois do ajuste. Usa arquivos temporários fictícios; cobre sucesso, atraso, corrupção, falha, preservação do último sucesso, concorrência, bloqueio órfão e caminho inválido. Não executa dump/restore real; ensaio de banco aprovado em 11/09 não foi repetido nesta sessão.
- Documentados comandos, variáveis, status privado, saída de falha, tratamento de bloqueio após interrupção e limites. Não há envio de notificações, agendamento, retenção ou criptografia implementados por essa rotina.
- Nenhuma migração, alteração em dados reais, backup real ou deploy. `ss -ltnp` não mostrou banco/API/interface nas portas 5432/3001/5173; nenhum processo iniciado. Git continua sem repositório utilizável.
- Próximo trabalho independente: ensaio operacional da aplicação recuperada em banco isolado. Destino externo, frequência/retenção, responsável e mecanismo de alerta ainda precisam ser definidos antes de ativar proteção automática.
- Formatação dos arquivos desta entrega aprovada após ajustar o novo teste com Prettier.

## Continuação autorizada: ensaio operacional

- Usuário autorizou continuar com o ensaio da aplicação recuperada.
- Implementado `tests/recovery.mjs`, chamado por `test:backup` depois da comparação integral do banco restaurado. Inicia API na porta 3011 exclusivamente sobre `salao_restore_ensaio` na instância descartável 15439.
- Ensaio compara consultas de clientes, comandas, produtos, financeiro, recebimentos e estoque com respostas da API de origem; verifica novo login, sessão anteriormente revogada, cadastro auditado e logout. Inventário integral da origem é novamente comparado após operações na cópia.
- Build aprovado; integração/backup em execução neste marco. Resultado final ainda pendente. Nenhuma migração ou escrita no banco real.

### Resultado final

- `npm run test:backup` aprovado com POSTGRES_BIN e LD_LIBRARY_PATH locais: build, integração completa de API, comparação integral do dump restaurado, operações da API recuperada e origem intacta. Nenhuma falha nessa execução.
- Não houve navegador, benchmark de RPO/RTO, cópia externa, backup real ou deploy. Nenhuma migration aplicada ao salão. O executor encerrou as APIs e o PostgreSQL descartáveis.
- Procedimento e roadmap atualizados. Próximos passos: aceite de recuperação pelo navegador, definição de destino externo/política/alertas e medição com volume representativo.

## Continuação: navegador sobre a cópia restaurada

- Usuário autorizou continuar. Adicionado `test:recovery`, que executa build, integração, backup/restauração, ensaio da API e navegador sobre a cópia. `test:backup` mantém o ensaio sem navegador.
- Novo `tests/recovery-browser.mjs`: interface isolada na porta 5178 com proxy para API recuperada 3011, login, busca do cliente exclusivo da cópia, novo cadastro e recarga, relatório financeiro conciliado com baseline, desktop/celular e logout. Capturas previstas `recovery-desktop.png` e `recovery-mobile.png` em `.local/screenshots`.
- Portas conferidas antes de iniciar. Ensaio em execução neste marco; ainda sem resultado. Nenhum acesso ao banco real.

### Navegador recuperado aprovado

- `npm run test:recovery` passou na primeira execução: build, integração completa de API, backup/restauração, ensaio da API recuperada e navegador. Cadastro persistiu após recarga; recebido 10,31, estornado 6,05 e líquido 4,26 coincidiram com baseline fictício. Logout persistiu após recarga. Sem erros de JavaScript e sem transbordamento horizontal no celular.
- Comparação integral confirmou origem intacta após operações na cópia. Capturas desktop/celular geradas; captura móvel inspecionada visualmente.
- Documentação atualizada; nenhum backup real, migration no salão, agendamento, cópia externa ou deploy. Próxima etapa depende de definir infraestrutura/destino externo e política de backup; medição com volume representativo e aceite operacional do responsável continuam pendentes.

## Preparação do agendamento e preferência por gratuidade

- Usuário autorizou seguir e, ao ser perguntado sobre infraestrutura, disse não saber onde hospedar e pediu indicação sem cobrança.
- Encontradas quatro unidades systemd já existentes. Documentadas em `ops/systemd/README.md`; criados exemplos de configuração. Serviço de verificação passou a usar `/etc/salao-backup-check.env`, sem senha de banco, e UMask 0077.
- `systemd-analyze verify` apontou `/usr/bin/node` inexistente no workspace; instalação/execução das unidades não validadas. `systemd-analyze calendar` aceitou 03h no fuso America/Sao_Paulo. Nada instalado ou habilitado, nenhum dado real acessado.
- Consultadas fontes oficiais para a resposta: https://support.google.com/drive/answer/6374270?hl=pt-BR (até 15 GB compartilhados), https://render.com/docs/free (inatividade, limites e orientação de não usar em produção). Também pesquisados Neon e Backblaze, sem contratação ou seleção.
- Proposta ainda não aceita: aplicação no computador e backups criptografados no Drive, sujeitos ao espaço livre. Integração e criptografia ainda não implementadas. Necessidade de acesso externo deve orientar a decisão de hospedagem.

### Esclarecimento de escopo: banco e backup

- Usuário informou intenção de registrar endereço HTTPS e pediu orientação apenas sobre banco e backup. A proposta local anterior não foi aceita.
- Recomendada combinação Neon Free (PostgreSQL) e backups criptografados no Drive; ainda apenas proposta, sem conta/configuração/migração/envio. Frequência e retenção serão dimensionadas conforme uso e tamanho.
- Fonte oficial consultada: https://github.com/neondatabase/website/blob/main/content/faqs/free-plan-limits-and-quotas.md, atualizada em 02/09/2026. Informa 0,5 GB, 100 CU-h e 5 GB de transferência por projeto/mês (armazenamento é limite contínuo); sem cobrança de excedentes no Free, porém limites podem suspender compute ou bloquear crescimento. Histórico de restauração curto não substitui backup externo.
- Pendente identificar hospedagem da API/agendador e medir tamanho do banco antes de qualquer migração. Nenhuma ação sobre dados reais.

### Hospedagem escolhida: Vercel

- Usuário informou que hospedará na Vercel. Nenhuma publicação solicitada ou realizada neste esclarecimento.
- Revisados main.ts e dependências: API NestJS com Prisma, servidor local; adaptação à implantação Vercel ainda não implementada/validada. Backups pg_dump e modelos systemd precisam de executor apropriado, ainda não escolhido.
- Fontes oficiais consultadas: https://vercel.com/docs/plans/hobby e https://vercel.com/docs/limits/fair-use-guidelines (Hobby restrito a uso pessoal não comercial); https://vercel.com/kb/guide/ship-a-nestjs-app-on-vercel (suporte NestJS); https://vercel.com/docs/cron-jobs/usage-and-pricing (cron executa funções e respeita limites).
- Neon Free e Drive continuam recomendações, não decisões aceitas. Esclarecer uso do plano Pro na Vercel antes de prometer solução sem mensalidade para hospedagem comercial. Sem mudanças em dados reais.

### Decisão sobre o plano Vercel

- Usuário escolheu Vercel gratuito inicialmente e migração para Pro ao começar a comercialização do site.
- Registrado planejamento do Hobby para desenvolvimento/demonstração não comercial. Esclarecido que operar o sistema no salão também é uso comercial, mesmo antes de vender o software.
- Próxima etapa: preparação e validação da aplicação/API para Vercel usando dados de teste. Nenhuma publicação, contratação, criação de conta ou migração dos dados reais foi realizada. Neon Free e Drive continuam propostas a configurar.

## Envio ao GitHub e hospedagem solicitados

- Usuário pediu subir o código ao seu GitHub e hospedar na Vercel; autorização de publicação agora explícita, sem incluir dados reais ou credenciais.
- Git inicializado com aprovação na branch `main`; gitignore ampliado para `.vercel/`, dumps e arquivos de chave. Inventário de 186 arquivos elegíveis revisado; busca por padrões comuns de tokens/chaves não encontrou ocorrências (não equivale a auditoria exaustiva). `.env` e `.local` confirmados ignorados.
- `gh` não está instalado. Habilidade plugin-management usada para descobrir GitHub/Vercel; ambos disponíveis, mas conexão ainda não confirmada. Solicitados repositório de destino e conexão das contas. Não pedir senha/token pelo chat.
- Documentação Vercel confirma suporte NestJS; combinação com frontend, cookies de mesma origem, Prisma e banco remoto ainda requer preparação/validação. Não houve push, deploy, migração ou envio de dados reais.

- Commit inicial local `0e86f90` criado com 186 arquivos na branch `main`. Verificação de whitespace aprovada após retirar linha vazia final em unidade systemd. Não repetidos testes funcionais, pois não houve alteração funcional. Conexões e destino continuam pendentes; sem push/deploy. Notas posteriores ao commit ficam para próximo salvamento Git.

- Usuário forneceu destino explícito `https://github.com/SantosMarcos95/Gestor-salao.git`. `git ls-remote` retornou sucesso sem referências: destino vazio nesta verificação. Primeira tentativa teve bloqueio de DNS no sandbox; execução autorizada com rede passou. Preparação de origin e push em andamento, sem resultado ainda.

- Origin configurado e commit `9797416` criado. Push autorizado falhou por ausência de autenticação HTTPS (`could not read Username for https://github.com`). Não houve envio nem deploy. Acesso público de leitura ao repositório não concede escrita. Pendente autenticação segura da conta para repetir push; nota final fica para próximo commit.

### Plugin conectado; escrita recusada

- Usuário informou plugin instalado e perguntou como conectar. Ferramenta solicitou autenticação, aceita; repetição confirmou login `SantosMarcos95`. Consulta ao repo confirmou permissões push/admin da conta e visibilidade pública.
- Git HTTPS continua sem credenciais. Tentativa de criar README pelo plugin foi recusada com 403 `Resource not accessible by integration`; permissão da conta não equivale à concessão da integração. Nada publicado.
- Próximo passo: autenticação Git via gh no terminal (CLI ainda ausente) ou ajuste das permissões da integração. Preparados 186 arquivos rastreados em `/tmp/salao-publish.json`, sem credenciais ou dados locais. Não repetir pedido de instalar/conectar plugin, pois conexão está confirmada.

### GitHub publicado

- Usuário concluiu autenticação do GitHub CLI pelo fluxo device em `github.com/login/device`.
- `gh auth status` confirmou `SantosMarcos95`, protocolo HTTPS e escopo de repositório. Commit `a7f854c` criado com os registros e `git push -u origin main` concluído: nova branch `main` publicada em `SantosMarcos95/Gestor-salao`.
- Repositório público vazio recebeu os 186 arquivos rastreados; `.env`, `.local`, dumps e chaves permanecem ignorados. Sem deploy Vercel, migração de banco ou dados reais enviados.
